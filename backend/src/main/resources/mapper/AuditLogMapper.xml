<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inventory.mapper.AuditLogMapper">

    <resultMap id="BaseResultMap" type="com.inventory.entity.AuditLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="operation" property="operation"/>
        <result column="content" property="content"/>
        <result column="method" property="method"/>
        <result column="ip" property="ip"/>
        <result column="user_agent" property="userAgent"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectPageList" resultMap="BaseResultMap">
        SELECT * FROM sys_audit_log
        <where>
            <if test="query.startDate != null">
                AND create_time &gt;= #{query.startDate}
            </if>
            <if test="query.endDate != null">
                AND create_time &lt;= #{query.endDate}
            </if>
            <if test="query.username != null and query.username != ''">
                AND username LIKE CONCAT('%', #{query.username}, '%')
            </if>
            <if test="query.operation != null and query.operation != ''">
                AND operation = #{query.operation}
            </if>
            <if test="query.keyword != null and query.keyword != ''">
                AND content LIKE CONCAT('%', #{query.keyword}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectByDateRange" resultMap="BaseResultMap">
        SELECT * FROM sys_audit_log
        <where>
            <if test="startTime != null">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
