<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inventory.mapper.MenuMapper">

    <resultMap id="BaseResultMap" type="com.inventory.entity.Menu">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="icon" property="icon"/>
        <result column="sort" property="sort"/>
        <result column="permission" property="permission"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectMenuTree" resultMap="BaseResultMap">
        SELECT * FROM sys_menu WHERE status = '1' ORDER BY sort ASC
    </select>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT m.* FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId} AND m.status = '1'
        ORDER BY m.sort ASC
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT m.* FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.status = '1'
        ORDER BY m.sort ASC
    </select>

    <select id="countByPermission" resultType="int">
        SELECT COUNT(*) FROM sys_menu WHERE permission = #{permission}
    </select>

</mapper>
