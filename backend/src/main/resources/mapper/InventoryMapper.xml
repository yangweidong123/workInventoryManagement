<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inventory.mapper.InventoryMapper">

    <resultMap id="BaseResultMap" type="com.inventory.entity.Inventory">
        <id column="id" property="id"/>
        <result column="style_no" property="styleNo"/>
        <result column="name" property="name"/>
        <result column="size_mm" property="sizeMm"/>
        <result column="weight_kg" property="weightKg"/>
        <result column="box_spec" property="boxSpec"/>
        <result column="price_excl_tax" property="priceExclTax"/>
        <result column="guide_price" property="guidePrice"/>
        <result column="min_price" property="minPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="DetailResultMap" type="com.inventory.dto.InventoryDTO">
        <id column="id" property="id"/>
        <result column="style_no" property="styleNo"/>
        <result column="name" property="name"/>
        <result column="size_mm" property="sizeMm"/>
        <result column="weight_kg" property="weightKg"/>
        <result column="box_spec" property="boxSpec"/>
        <result column="price_excl_tax" property="priceExclTax"/>
        <result column="guide_price" property="guidePrice"/>
        <result column="min_price" property="minPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="goods_value" property="goodsValue"/>
        <result column="total_weight" property="totalWeight"/>
    </resultMap>

    <select id="selectPageList" resultMap="DetailResultMap">
        SELECT
            id,
            style_no,
            name,
            size_mm,
            weight_kg,
            box_spec,
            price_excl_tax,
            guide_price,
            min_price,
            quantity,
            quantity * price_excl_tax AS goods_value,
            quantity * weight_kg AS total_weight
        FROM inventory
        <where>
            <if test="query.styleNo != null and query.styleNo != ''">
                AND style_no LIKE CONCAT('%', #{query.styleNo}, '%')
            </if>
            <if test="query.name != null and query.name != ''">
                AND name LIKE CONCAT('%', #{query.name}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectDetailById" resultMap="DetailResultMap">
        SELECT
            id,
            style_no,
            name,
            size_mm,
            weight_kg,
            box_spec,
            price_excl_tax,
            guide_price,
            min_price,
            quantity,
            quantity * price_excl_tax AS goods_value,
            quantity * weight_kg AS total_weight
        FROM inventory
        WHERE id = #{id}
    </select>

</mapper>
